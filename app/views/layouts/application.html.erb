<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>
  <body>
    <div class="bg-primary text-center text-white mb-4 p-4">
      <%= link_to root_path, class: 'text-white' do  %>
        <h1 class="display-4">MarcosDev.io</h1>
      <% end %>
      <p class="lead">
        Olá, aqui você encontrará artigos sobre  Ruby, Typescript, C#, Tecnologia e Carreiras.
      </p>
    </div>
    <div class="container">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == 'alert' ? 'danger' : 'success' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close"  data-bs-dismiss="alert" aria-label="Close">
          </button>
        </div>
      <% end %>
      <div class="row">
        <div class="col-4 offset-8">
          <div class="search">
            <%= form_with url: search_posts_path, method: :get, local: true, class: 'form-inline' do |form| %>
              <div class="mx-3 mb-3">
                <%= form.search_field :q, class: 'form-control', required: true, placeholder: 'Buscar', value: params[:q] %>
              </div>
              <%= form.submit 'Buscar', class: 'btn btn-primary mb-3', data: { disable_with: 'Buscando...' } %>
            <% end %>
          </div>
        </div>
      </div>
      <%= yield %>
    </div>
    <footer class="mt-5 bg-primary text-center p-4" style="bottom: 0">
      <div class="position-relative d-flex justify-content-center p-2">
        <p>Copyright @ Marcos Tomaz <%= Time.now.year %></p>
      </div>
    </footer>
  </body>
</html>
